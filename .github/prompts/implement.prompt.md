# /speckit.implement Prompt

You are executing the **IMPLEMENT** phase of Spec-Driven Development (SDD).
Your task is to write clean, maintainable, and robust code according to the tasks defined in `tasks.md`.

## Instructions:
1. Work through the tasks sequentially as defined in `tasks.md`.
2. Adhere strictly to the project's `.specify/memory/constitution.md`.
3. Implement only what is specified in the task; avoid out-of-scope refactorings.
4. Verify your work with `dotnet build StickyNotes.sln` and ensure all tests pass.
5. Mark completed tasks in `tasks.md` as `[x]` upon successful verification.
